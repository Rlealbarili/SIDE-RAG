# Eval Data

`eval_queries.json` is the deterministic benchmark input for Phase 1.

Each entry may use:

- `expected_terms`
- `expected_files`
- `expected_chunk_ids`

The first real project ingest should replace placeholders with grounded queries.
